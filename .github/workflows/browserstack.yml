name: 'BrowserStack Test'
on: [push, pull_request]
jobs:
  ubuntu-job:
    name: 'BrowserStack Test on Ubuntu'
    runs-on: ubuntu-latest  # Can be self-hosted runner also
    steps:

      - name: 'BrowserStack Env Setup'  # Invokes the setup-env action
        uses: browserstack/github-actions/setup-env@master
        with:
          username:  ${{ secrets.BROWSERSTACK_USERNAME }}
          access-key: ${{ secrets.BROWSERSTACK_ACCESS_KEY }}

# The next 3 steps are for building the web application to be tested and starting the web server on the runner environment

      - name: 'Checkout the repository'
        uses: actions/checkout@v2

      - name: 'Installing all the dependencies'
        run: npm install 

      - name: 'Installing browserstack-cypress-cli'
        run: npm install -g browserstack-cypress-cli
        
      - name: 'Installing percy-cli'
        run: npm install -g @percy/cli
        
      - name: 'Switch to current branch'
        run: git checkout cypress-with-percy

      - name: 'Running test on BrowserStack'  # Invokes the actual test script that would run on BrowserStack browsers
        run: PERCY_TARGET_COMMIT=76dec047f971fe7f057335ba978eab04e7121322 PERCY_TOKEN=${{ secrets.PERCY_TOKEN }} npx percy exec -- browserstack-cypress run --sync  # See sample test script above
